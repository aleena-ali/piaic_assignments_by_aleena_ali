"use strict";
let array = ["car", "motorcycle", "bus", "train"];
let statement = "I wish to own ";
for (let index = 0; index < array.length; index++) {
    var vehicle = array[index];
    console.log(statement + vehicle);
}
